#!/usr/bin/env ruby

require 'obj'

puts "OBJ version #{OBJ::VERSION}"
file_path = ARGV[0]
if file_path && File.exist?(file_path)
  puts OBJ::Mesh::read(file_path)
else
  puts "<file_path> required"
end
